name: my_firebase_amraa_teacher
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish

version: 1.0.0+1

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  cloud_firestore:
  firebase_core:
  firebase_database:
  intl:
  http:

  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  path_provider:
  dio:
  g_json:
  get:
  hive:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
